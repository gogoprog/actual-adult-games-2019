package game;

import whiplash.math.*;

class Machine {
    public var time:Float = 0.0;
    public var position = new Vector2(0, 0);
    public var reachedPosition = new Coord(0, 0);

    public function new() {
    }
}
